server:
  port: 80


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://${MYSQL_ADDRESS}/${MYSQL_DATABASE:cookie}
#    username: ${MYSQL_USERNAME}
#    password: ${MySQL_PASSWORD}
    url: jdbc:mysql://localhost:3306/cookie
    username: root
    password:
  mvc:
    view:
      suffix: .html
      prefix: /

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml


elsticserach:
  rest:
    uris:
    username: elastic
    password: tAYzComWgpM8_BKaLwC_